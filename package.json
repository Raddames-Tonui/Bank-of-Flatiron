{
  "homepage": "https://Raddames-Tonui.github.io/Bank-of-Flatiron",
  "name": "react-hooks-bank-of-flatiron-code-challenge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-icons": "^5.1.0",
    "react-scripts": "^4.0.1",
    "semantic-ui-css": "^2.2.10"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "PORT=8000 react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "server": "json-server --watch db.json -p 8001"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
